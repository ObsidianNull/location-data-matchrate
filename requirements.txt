requests    # HTTP layer used for making API calls, useful for the sequential and rate-limited nature of the script

tenacity    # used for retrying failed API calls, especially when dealing with rate limits or transient network issues

pandas      # used for data manipulation and analysis, particularly for handling tabular data returned from API calls

PyYAML # used for parsing YAML configuration files, which can be useful for managing settings and parameters for the script (specifically helps maintain the fiscal-year dataset ID table not hardcoded without overengineering)

python-dotenv # used to load environment variables from a .env file, which is useful for managing sensitive information like API keys without hardcoding them into the script

openpyxl    # used for reading and writing Excel files, which can be useful for exporting data or reports generated by the script specifically to maintain a single workbook with raw and summary sheets

pytest # used for testing the script, ensuring that the code behaves as expected and helping to catch bugs early in the development process

ruff # used for linting and code quality checks, helping to maintain a clean and consistent codebase by enforcing coding standards and best practices
